version: '3'
services:
  api:
    image: "node:18"
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV="development"
    volumes:
      - ./:home/node/app
    ports:
      - "3004:3004"
    command: "npm run start"
